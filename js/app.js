document.addEventListener('DOMContentLoaded',()=>{initCharts();clock();setInterval(()=>{let d=generate();render(d);push(d.power);clock()},1000);reset.onclick=()=>{if(confirm('Reset energy?')){meter.energy=0;render(meter)}};clear.onclick=()=>{if(confirm('Clear data?')){meter.energy=0;meter.month=0;render(meter)}};export.onclick=()=>{let csv='Parameter,Value\nVoltage,'+meter.voltage+' V\nCurrent,'+meter.current+' A\nPower,'+meter.power+' W\nEnergy,'+meter.energy+' kWh';let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download='energy-meter.csv';a.click()};render(meter)});function render(d){let e=Math.max(0,d.energy),c=e*TARIFF,p=Math.min(100,e/LIMIT*100),va=d.voltage*d.current,pf=d.power/va;set('voltage',d.voltage.toFixed(1)+' V');set('current',d.current.toFixed(2)+' A');set('power',d.power.toFixed(1)+' W');set('energy',e.toFixed(2)+' kWh');set('cost',rp(c));set('todayCost','Rp '+rp(c));set('projection','Rp '+rp(c*30));set('percent',Math.round(p)+'%');set('donut',e.toFixed(2));set('bar','');document.getElementById('bar').style.width=p+'%';set('va',va.toFixed(1)+' VA');set('pf',Math.min(1,pf).toFixed(2));set('forecast',(e*1.18).toFixed(1));set('forecastMonth',Math.round(e*30)+' kWh');set('forecastBill','Rp '+rp(e*30*TARIFF));set('last',new Date().toLocaleTimeString());energyChart.data.datasets[0].data=[Math.min(e,LIMIT),Math.max(0,LIMIT-e)];energyChart.update('none')}function set(id,v){let e=document.getElementById(id);if(e)e.textContent=v}function rp(v){return Math.round(v).toLocaleString('id-ID')}function clock(){let n=new Date();set('clock',n.toLocaleTimeString());set('date',n.toLocaleDateString('en-GB',{weekday:'short',day:'2-digit',month:'short',year:'numeric'}))}
