let powerHistory=[],powerLabels=[],powerCtx,energyCtx,weeklyCtx;function setupCanvas(id){const c=document.getElementById(id);if(!c)return null;const r=c.getBoundingClientRect(),d=window.devicePixelRatio||1;c.width=Math.max(10,r.width*d);c.height=Math.max(10,r.height*d);const x=c.getContext("2d");x.setTransform(d,0,0,d,0,0);return{x,w:r.width,h:r.height}}function initCharts(){for(let i=29;i>=0;i--){let d=new Date(Date.now()-i*120000);powerLabels.push(d.toLocaleTimeString([],{hour:"2-digit",minute:"2-digit"}));powerHistory.push(120+Math.random()*100)}powerCtx=setupCanvas("powerChart");energyCtx=setupCanvas("energyChart");weeklyCtx=setupCanvas("weekly");drawPower();drawEnergy(12.46);drawWeekly()}function drawPower(){let q=setupCanvas("powerChart");if(!q)return;powerCtx=q;let{x,w,h}=q,c=x,p=powerHistory,pl=powerLabels,pad={l:42,r:12,t:12,b:28},cw=w-pad.l-pad.r,ch=h-pad.t-pad.b,max=Math.max(300,Math.ceil(Math.max(...p)/50)*50);c.clearRect(0,0,w,h);c.strokeStyle="#e6edef";c.fillStyle="#62757b";c.font="11px Arial";for(let i=0;i<=4;i++){let y=pad.t+ch*i/4;c.beginPath();c.moveTo(pad.l,y);c.lineTo(w-pad.r,y);c.stroke();c.fillText(Math.round(max*(1-i/4)),7,y+4)}let pts=p.map((v,i)=>({x:pad.l+i*cw/(p.length-1),y:pad.t+ch-v/max*ch}));c.beginPath();pts.forEach((a,i)=>i?c.lineTo(a.x,a.y):c.moveTo(a.x,a.y));c.lineTo(pts.at(-1).x,pad.t+ch);c.lineTo(pts[0].x,pad.t+ch);c.closePath();c.fillStyle="rgba(8,127,140,.13)";c.fill();c.beginPath();pts.forEach((a,i)=>i?c.lineTo(a.x,a.y):c.moveTo(a.x,a.y));c.strokeStyle="#087f8c";c.lineWidth=3;c.stroke();p.forEach((_,i)=>{if(i%5===0){c.fillStyle="#62757b";c.fillText(pl[i],pts[i].x-12,h-7)}})}function drawEnergy(v){let q=setupCanvas("energyChart");if(!q)return;energyCtx=q;let{x,w,h}=q,c=x,cx=w/2,cy=h/2,r=Math.min(w,h)*.34,p=Math.min(1,Math.max(0,v/LIMIT));c.clearRect(0,0,w,h);c.lineWidth=18;c.lineCap="round";c.strokeStyle="#dfe8e9";c.beginPath();c.arc(cx,cy,r,0,Math.PI*2);c.stroke();c.strokeStyle="#087f8c";c.beginPath();c.arc(cx,cy,r,-Math.PI/2,-Math.PI/2+Math.PI*2*p);c.stroke()}function drawWeekly(){let q=setupCanvas("weekly");if(!q)return;weeklyCtx=q;let{x,w,h}=q,c=x,d=[10.2,12.5,14.8,11.6,16.4,18.1,12.5],labels=["Aug 4","Aug 5","Aug 6","Aug 7","Aug 8","Aug 9","Aug 10"],pad={l:35,r:10,t:10,b:30},cw=w-pad.l-pad.r,ch=h-pad.t-pad.b,max=20;c.clearRect(0,0,w,h);c.strokeStyle="#e6edef";c.fillStyle="#62757b";c.font="11px Arial";for(let i=0;i<=4;i++){let y=pad.t+ch*i/4;c.beginPath();c.moveTo(pad.l,y);c.lineTo(w-pad.r,y);c.stroke()}let gap=8,bw=(cw-gap*6)/7;d.forEach((v,i)=>{let bh=v/max*ch,x0=pad.l+i*(bw+gap),y=pad.t+ch-bh;c.fillStyle="#2aa6b2";c.beginPath();c.roundRect(x0,y,bw,bh,5);c.fill();c.fillStyle="#62757b";c.fillText(labels[i],x0-3,pad.t+ch+20)})}function pushPower(v){powerHistory.push(v);powerHistory.shift();powerLabels.push(new Date().toLocaleTimeString([],{hour:"2-digit",minute:"2-digit"}));powerLabels.shift();drawPower()}window.addEventListener("resize",()=>{drawPower();drawEnergy(parseFloat(document.getElementById("donut")?.textContent)||0);drawWeekly()})